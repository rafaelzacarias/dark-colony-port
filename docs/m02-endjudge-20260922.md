# Independent M02 Outcome Rerun

## Verdict

**M02 milestone: FAIL. Full game: FAIL / not accepted.** HUMAN02 reached a
runtime diagnostic before its original victory. Completion of all 30 missions
has not been demonstrated. Startup, economic activity, or AI activation alone
cannot substitute for the two required ready victories.

This rerun uses the actual original loader, initialized source assets and public
selection, movement/assault, harvesting and paid-production commands. It uses
Node/NullCanvas, not a browser. No runtime, original source, AI configuration,
objective counter, health, credits or visibility was edited. No agents were used.

| Actual outcome | HUMAN02 | ALIEN02 |
| --- | --- | --- |
| Ready WIN / ready LOSS | Neither | Neither |
| Stop tick | 17877 | 17760 |
| Original objective | 11/27 | 4/6 |
| Unique deaths / shots | 71 / 3595 | 60 / 2313 |
| Paid purchases / spent | 15 / 5250 | 11 / 3850 |
| Earned / credits | 5280 / 30 | 3875 / 25 |
| Original source hashes | Unchanged | Unchanged |
| Final JSON checkpoint equality | PASS, diagnostic state | PASS, no mission outcome |
| Failure | Production ownership rejection | 900-second QA simulation deadline |

The generated [result artifact](m02-endjudge-results-20260922.json) contains
provenance, exact objectives, reserve/accounting checks and selector observations.

## First Blocking Failure

HUMAN02 stopped at simulation tick **17877**, objective **11/27**, outcome null:

```text
Production colony mutation requires native destruction/construction ownership
```

The last unique combat event killed team-0 production actor **19** at **(54,55)**
on combat tick 17876, visible in simulation snapshot 17877. The following update
rejected the production/world health mismatch in
[synchronizeProductionCredits](../src/engine/campaign-session.ts#L2098).
This is neither a ready LOSS nor a ready WIN. No runtime repair or tactical
rerun was applied to conceal the failure.

Follow-up: [browser-adapted production destruction](browser-production-destruction-20260922.md)
adds a separate destruction/cancellation owner while preserving strict-native
guards. The actual healthy tick-4110 save was resumed through the original
public-command journal; all 17876 pre-death session inputs match this run.
Continuation reaches 17900 without the diagnostic, with slot 0/type 16 at zero
health, one source loss, and unchanged 5280 income / 5250 spent / 30 credits.
The objective remains 11/27 and outcome remains null because slot 1 survives.
This resolves the reported blocker, not this historical M02 acceptance failure.

Evidence: `/tmp/dc-m02-human-win-eHErIu/journal.jsonl` and its adjacent
`checkpoint.json`; launcher `/tmp/dc-m02-endjudge-20260922-160602.log`.
The final checkpoint round-tripped exactly, but it contains the diagnostic:
this is not healthy post-victory continuation evidence.

HUMAN had **71 unique deaths**, **3595 unique shots**, 15 paid purchases,
5250 spent, 5280 earned/published and 30 credits. The original reserve audit is
26000 initial minus 20720 remaining equals 5280 earned. Original SCN, TRO, TXT,
MAP, MTG and PTH hashes remain unchanged. The QA observer repeats the last combat
frame after the diagnostic stalls advancement, producing raw totals 72/3596;
the independent report deduplicates complete combat event records.

The harvester died at tick 4952 while most troops were away from the economy.
Natural HUMAN AI activation occurred at tick 14096; the run did not shorten the
source clock. Progress reached 1000/5000/10000/14096/17000 ticks in approximately
18/125/294/475/630 seconds. The complete HUMAN test, including full JSON restore,
took 902 seconds. These are strategy/observation times, not isolated update
benchmarks or a claim of constant 15-ms frames.

## ALIEN Result

ALIEN02 stopped at tick **17760**, objective **4/6**, with null outcome and
`QA wall-clock deadline exceeded (900000ms)`. There was no source-runtime
diagnostic. Its original harvester died on combat tick 3255 (target 50).
At tick 14120 only the recovered commander remained alive on the player side;
the remaining goal sites at (93,53) and (89,51) each had 250 HP. With no harvester,
no expedition and 25 credits, the protected-commander strategy made no further
objective progress. This is an unsuccessful strategy, not an original LOSS.

The original two type-41 turrets fired 54 shots. Natural team-1 AI activation
occurred at 1136. Income was 3875, all published; eleven purchases cost 3850.
The original reserve audit is 18000 minus 14125 equals 3875. The final checkpoint
round-tripped exactly after activation, without creating a mission outcome.
Journal and checkpoint: `/tmp/dc-m02-alien-win-RVnc6P/`.

The already-loaded pre-refinement strategy ran both factions. No second attempt
was started after the HUMAN runtime blocker was established.

## Timing And Exit

The 900-second simulation deadline excludes synchronous checkpoint restoration.
ALIEN's complete case took 1121.88 seconds, about 222 seconds beyond that cap;
HUMAN's complete case took 902.33 seconds. Thus the requested 900-second total
per-case wall bound was **not met** by this existing harness. The launcher
reported code 1 with `ETIMEDOUT`; its timeout did not prevent both terminal
result records, both checkpoint-verification events and the test footer from
being written. The footer reports eight source controls passing and two actual
playthrough assertions failing, with total test duration 2024.37 seconds.

No successful timing-bound claim is made. The final process check found neither
the owned launcher (PID 71428), its test child (PID 71429), nor an exit observer
remaining. No process was left running at handoff.

## Verified Controls

- Three fresh controls pass: HUMAN and ALIEN natural delivery, finite-source
  earned production, mid-extraction JSON restore with exact continuation;
  ALIEN natural activation at 1136 and post-activation restore/continuation.
  Log: `/tmp/dc-endjudge-controls-1790118973570.log`.
- Five focused depletion controls pass, including synthetic concurrent-extractor
  cases and exact finite depletion/no subsequent income. These are control
  tests, not synthetic mission victories.
  Log: `/tmp/dc-endjudge-depletion-1790119443988.log`.
- The earlier HUMAN session-level natural threshold/replay test at 14096 passes
  in `/tmp/dc-m02-human-activation-17901134.log` (455 seconds). This existing
  evidence was checked, not rerun or relabeled as a new public-view victory.
- Nine source/QA strategy controls and scoped strict TypeScript pass in
  `/tmp/dc-endjudge-strategy-checked-1790119390045.log`.

## Source Criteria

HUMAN's original trigger 6 is `(s(2,3)>26)`: 27 team-2 victim losses, not a
count of arbitrary hostile bases. Ten initial actors plus the seventeen original
reinforcements at `c>880` supply that census. ALIEN's trigger 4 is
`(s(1,0,86)>5)`: six team-1 type-86 losses.

Both scripts define base-loss bail actions. HUMAN also loses a player type-86
site. Neither script defines commander death alone as LOSS: the first death
schedules recovery, and a second commander death does not add a loss bail.
There is no commander-exposure LOSS requirement in this milestone. Existing
original loss conditions are not removed or weakened.

## QA Changes And Limits

Before the runtime diagnostic was discovered, a single tactical revision was
prepared: six economic defenders, a separate expedition and a matching minimum
recruitment target. It passed focused tests but was **not used by the running
process**, which had already imported the strategy. No successful end-to-end
validation of that revision is claimed. Disk hashes captured at ALIEN startup
may show this later QA file, not the cached strategy executing in that process.

[The independent reporter](../tools/qa/m02-endjudge-report.mjs) reads completed
journals, checks raw source hashes, currency/reserve accounting, unique combat
events, selector evidence and final checkpoint verification. It returns a
failing exit code unless both original missions have ready victories and the
required accounting/save evidence. Missing terminal records remain explicitly
INCOMPLETE, with last-observed accounting labeled by observation tick. Complete
and truncated-journal controls pass; both actual journals here have terminal
records. It never changes game state.

Reproduce the read-only report with:

```sh
node tools/qa/m02-endjudge-report.mjs /tmp/dc-m02-human-win-eHErIu/journal.jsonl /tmp/dc-m02-alien-win-RVnc6P/journal.jsonl
```

The expected report exit code is 1. Main-owner priority is the production
destruction/world synchronization rejection above; do not change original TRO
victory/loss conditions to make this acceptance pass.

No browser input/pixels, audio, persistent browser save, result-screen navigation,
next-mission progression, native timing parity or complete campaign is claimed.