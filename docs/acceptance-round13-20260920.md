# Round 13 Independent Acceptance Audit

Date: 2026-09-20. Scope: documentation-only review of existing evidence. This
audit changes only this report and the newest brief in
[ARCHITECTURE.md](../ARCHITECTURE.md). No agents, code changes, browser session or
full-suite execution were used. Older reports retain their historical claims;
this report supersedes only current acceptance status.

**All phases are NOT accepted. Original campaign mission 2 remains closed.**
Passing bounded contracts is not original scheduler or whole-campaign admission.

## Phase Matrix

| Phase | Acceptance | Round 13 disposition |
| --- | --- | --- |
| 1 | Accepted, pinned scope only | Existing source/provenance acceptance retained; not re-audited here. |
| 2 | Accepted, pinned scope only | Existing conversion acceptance retained; unsupported effects do not become globally supported. |
| 3 | Partial, NOT accepted | Source owners, AI, statics, transports and bounded combat/visibility exist; shared original native scheduling and broader lifecycle admission remain open. |
| 4 | Partial, NOT accepted | View projection, semantic orders, restore and bounded rendering have positive evidence; ordinary device input, audible playback and automatic render/clock ownership are not certified. |
| 5 | Partial, NOT accepted | Completed full check plus separate focused evidence; mobile/final cleanup and sustained performance/memory gates remain open. |

## Test Ledger

The audit read the actual log footers, not just the handoff's running status.

| Evidence | Verified result | Limit |
| --- | --- | --- |
| `/tmp/dc-round13-integrated-20260920.log` | 2,685 tests; 2,681 pass; 0 fail; 4 skip; 0 cancelled; 1,740,968.726208 ms | Latest completed full check, not a full check of later edits. |
| Same integrated log | Typecheck and build completed; JS 644.16 kB, gzip 202.11 kB; build 4.41s | Over-500-kB chunk warning remains. |
| `/tmp/dc-round13-final-focused-20260920.log` | 155 tests; 155 pass; 0 fail/skip/cancelled; 255,228.637625 ms | Running at handoff, completed when inspected; separate focused run, not another full. |

The later full-ground visibility extension and mode3 capture fix are not folded
into the completed full's coverage or counts. Focused selections may overlap
other evidence; do not sum them into a new distinct-test or full-suite total.
No later full typecheck/build pass is inferred from the focused test footer.

The focused log explicitly includes the 37-case ground matrix, full fresh
HUMAN02 producer host test and runtime guards, natural MissionView sound
callback, queued Move/Stop/attack-move and malformed-binding guards, and the
mode3 missing-parts rejection. This attributes those checks to the focused run,
not to the earlier full.

Browser observations below are MAIN's supplied QA evidence, not a browser rerun
by this auditor. Owner documents establish their own narrower Node/native scope;
historical statements that browser work was pending are not rewritten globally.

## Visibility And Sound

The [native owner](native-visibility-owner-20260920.md) now executes the actual
source tree and separate CITY/ground worker paths, not just the earlier 0/8
primitive. Fresh HUMAN02 has 21 original actors and 14 eligible producers, with
no exclusions or actor-status edits. The
[host/session evidence](source-native-visibility-host-20260920.md) matches 411
current sight and 411 explored cells and 10,487 ordered caller writes. All 21
original actors remain present. Source-normalized host raw fields must not be
misrepresented as identical native FIN pointers. The clear phase retains 411
explored with zero sight, and full recompute restores the original ground state.
MAIN reports full-producer browser success, exact restore and no diagnostic.

ALIEN02 has 44 actors and 41 eligible producers with no exclusions in pure/native
coverage only. The combat host provider remains HUMAN02-only. New ground evidence
is 37 cases and 1,038,548 ordered writes; the old 28 cases/196,473 writes remain
separate and unchanged, not 29 cases. Both oracle hashes were checked in this
audit. The preserved oracle is
`/tmp/dc-visibility-matrix-own31.jsonl`, SHA-256
`66bf4aeab4b08ce1d9e919ee871ea2298aee916bc29e427da8d7bd1cbe66a22c`.
The new matrix is `/tmp/dc-vis-ground-matrix-g21.jsonl`, SHA-256
`c206618bed6e596b5f7df9af07854dd3d5a4e4374857a7485dfe2d3a1ad8279b`.

The separate bounded [MissionView visibility](mission-native-visibility-20260920.md)
browser observation is 129 sight/129 explored, then 0/129, exact restore and
recompute 129/129 at actor tick 16. It does not run generic advance or establish
original world-service order. Do not conflate 129-cell and 411-cell fixtures.

Natural source exploration now reaches actual host death 614 and sound 28,
`TROPDEA3.WAV`, advancing the explicit CRT boundary 1 to 1103527590. Actual
MissionView integration delivers the callback exactly once after outer commit;
missing consumers/late projection failure roll back, throwing consumers do not
retry committed delivery, and fresh full-history restore/next death visit does
not repeat it. This is a positive natural host and full-view callback proof,
not merely controlled bit 31 or an isolated helper. The source visibility phase
itself consumes neither combat RNG nor CRT. The explicit initial audio boundary
does not establish arbitrary earlier original mission CRT consumers.

Still open: audible browser output, original full scheduler/all prior audio
history, dynamic alliance/daylight, unsupported producer states and wider combat
providers. Natural visibility and positive view callback are no longer missing.

## Commands And Performance

[Bounded player input](mission-native-player-orders-20260920.md) admits normal
Move 2 and Stop 13 handlers plus Attack 7 as bounded attack-move to a free endpoint,
not occupied-enemy target locking. One semantic command queues for the next
explicit native frame, revalidates source/slot/generation/raw identity and clears
only after commit. No automatic clock or general route admission is installed.
P2 sparse-array authentication is fixed: each of 220 entries must be an own
integer byte and match exactly; adapter and view reject malformed bindings.

MAIN's Playwright keyboard/pointer attempts delivered **zero events**. Synthetic
`isTrusted: false` M/click/S events queued Move 2, restored exactly and consumed
it once, then delivered Stop at tick 18. This is handler/API evidence only.
Ordinary keyboard/pointer input has NOT passed.

[Compact projection measurements](native-view-projection-20260920.md) compare the
same 20-frame caller against the one-copy intermediate, not a new original baseline:

| Measurement | Intermediate | Compact |
| --- | ---: | ---: |
| 20 advances | 7,597.982 ms | 5,160.971 ms |
| First five advances, mean | 211.500 ms | 88.170 ms |
| Public snapshot / compact JSON size | 14,725,895 bytes | 142,322 bytes |

Same complete frame 20 snapshot hash, checkpoint/replay/rollback guards and zero
public session snapshot reads are retained. Loaded FIN/atlas software Canvas
dispatch measured 0.141-0.285ms with cached per-revision sampling. That is not
decoded browser pixel/GPU timing, 60 FPS or a passed sub-50ms native-frame gate.
Allocation and transaction costs remain material; sustained memory is unproven.

## Mode3 Rendering

[Native mode3](mode3-native-20260920.md) is a terrain illumination prepass, not a
body blend. 192 native framebuffer variants and 65,536 arithmetic pairs match.
[The live bounded entry](live-bounded-mode3-20260920.md),
`renderBoundedMode3`, is opt-in and bounded to 128*1024 pixels. The caller must
supply initialization and complete queue/captures; the browser fixture uses
filter 128. Normal rendering does not automatically gain lighting, world admission
or global queue order. The default 512x452 viewport exceeds that bound.

MAIN reports a real-browser pass through the
[standalone fixture](mode3-browser-20260920.md): original VENT timeline 19, all four
children unchanged, 334 final changed pixels, 536 changed pre-body terrain pixels
and 228 surviving native-positive pixels. State, simulation and initial filter
remain unchanged; terrain publishes before bodies, with zero SMSP body draws.
The 228 browser surviving pixels are not the 334 software-Canvas surviving pixels
in the older owner report. GLIT retains its mode5 fallback and global ordering
remains unverified; neither is suppressed by this positive prepass result.

P2 found that a nonempty child capture with `parts: []` could disappear during
flattening and incorrectly appear exact. Per-entity completeness now rejects
before flattening, source lookup, filter staging or terrain publication. Empty
children/empty parts remain a valid empty capture. Nine integration tests pass,
with zero failures/skips, separately from the earlier full; evidence:
`/tmp/dc-mode3-missing-parts-integration-20260920-p3.log` and strict types
`/tmp/dc-mode3-missing-parts-strict-20260920-p4.log`.

## Remaining Gates

AI policies, static lifecycle owners and transports are implemented with bounded
proofs. The remaining integration gate is their source-owned shared native
TRO/AI/production/resources/combat schedule and RNG/history, not missing AI,
statics or transport code. Original HUMAN02/ALIEN02 admission stays closed.

Small next vertical: one ordinary keyboard/pointer Move then Stop through the
existing bounded queue, explicit frame consumption and exact queued restore.
Require actual delivered device events and once-only publication; do not replace
this with synthetic dispatch or enable an unproved automatic clock. Separately
verify audible browser playback of the already-proven postcommit death callback.

MAIN completed mobile Canvas checks: at viewport390, the 320-pixel mode3 canvas
fits; disabled output has zero changed pixels, enabled output has334 changed
pixels and228 surviving native-positive terrain pixels. Simulation, checkpoint
and initial illumination remain unchanged. The QA label/report overflow was
corrected; measured stage client/scroll widths are both360, label widths both336.
The embedded screenshot compositor returned a stale image after reload, so the
corrected layout is measured evidence, not a newly verified screenshot. PNG load
events replace a hidden-page `decode()` stall; four fixture contracts pass in
`/tmp/dc-round13-mode3-load-final-20260920.log`.

Final separate typecheck/build passed: JS652.67kB, gzip204.62kB, build4.23s;
the chunk-size warning remains. This is not another full test run. The shared
page returned to the launcher at width1440; all QA overlays/globals were removed
and the existing save retained.

Broader routes/types/weapons, automatic light/reset/admission and sorted
render order, sustained frame-time/memory budgets and original campaign scheduler
parity remain independent gates. No full phase is newly accepted by this audit.