# Phase Acceptance - 2026-09-19

## Latest Native-Task And Mode-5 Round

**Phases 1-2 retain acceptance; Phases 3-5 remain unaccepted.** Final gate:
**2,295 passed, zero failed, four skipped**, typecheck/build clean. Vite's
over-500-kB chunk warning remains an explicit packaging/performance concern.
See [round-10 integration evidence](acceptance-round10-20260919.md).

The old failing actor-task probe has been replaced by isolated original-source
fixtures and a bounded registered-visit implementation. Session/host ownership
now consumes authenticated actor orders, advances native turns/steps/reservations,
retains ownership through idle and checkpoints the exact raw/FIN/task state.
Review fixed direct-entry authentication bypass, invalid occupancy publication
and silently discarded sub-tick inputs. This does not admit arbitrary original
mission actors, combat acquisition, commander special Stop or shared scheduling.

Canonical SHA-256 receipt integrity closes low-level duplicate certification gaps;
legacy receipts are sealed only after full source recomputation. The pinned
hashing dependency and lockfile agree. Coherent caller rewriting remains outside
an authenticity claim; session source and transactional checks are retained.

Mode-5 effects are now live for the proved nonmirrored ground-level contract,
using the shipped RMP bank and source coverage. Embedded BEAC rendering changed
648 pixels with one exact write and no fallback image draw. Complete global
effects, mirrored/elevated cases, modes 2/3/4 and original campaign visual parity
remain unaccepted. Browser probes were disposed and the launcher restored.

All-phase enablement is still blocked by general native actor/world integration,
complete economy/construction/abilities, remaining rendering behavior and the
independent input/device/reference/performance gates. None is bypassed by an
acceptance-label change.

## Bounded Mode 5 Rendering Increment

Ground-level, nonmirrored flags-16/layer-0 mode-5 effects now run automatically
through the existing mission palette loader/frame adapter. The
[mode-5 report](phase4-mode5-effect-20260919.md) records exact BEAC/GLIT/GLAT
source cases, all four palettes, atomic fallback and the bank-1 indexed blend.
Mirrored effects, mode-3 pre-terrain drawing and native global ordering remain
gated. This advances the supported live parts without accepting Phase 4 or
changing mission admission, caller/overlay ordering or source assets.

## Latest Full-AI Computation And Shadows Round

**Phases 1-2 retain acceptance; Phases 3-5 remain unaccepted.** The user request
for all phases is still not fulfilled. Final gate: **2,035 passed, zero failed,
four skips**, clean typechecking/build and editor diagnostics. See the independent
[round-9 review](acceptance-round9-20260919.md) with final verification addendum.

AI initialization/preparation/demand and all four group computations now execute
in the verified source order, including group-1 retarget/release/create. A full
session transaction validates shared actor pools, commits ordered mode-5/7
receipts, preserves RNG/force state, and accepts prepaid mode-10 troops without
a second debit. Actual FIN producers allocate one unit and survive checkpoint
restore. Unsupported city mode 9 still rolls back. Original mode-3 mission
admission remains closed because targeted receipts await native actor task
consumption; clearing pending flags or assigning generic paths would hide that gap.

Native mode-1 shadows/body are now live through the existing indexed image/scene
adapter. Original RMP row 72 shades destination indices; no approximate ellipse
or alpha darkening is substituted. 256 native framebuffer cases cover four
palettes, mirrors and team selectors. Selection and health overlays moved after
world drawing; actual render results determine warnings. Unchanged HUMAN01 browser
renders produced six shadows selected/unselected. Measured synchronous draw work
was 9.6/11.5/14 ms p50/p95/max over 120 frames, not a 60-FPS or sustained-render gate.

Review fixes prevent full-policy configuration from downgrading to partial stages
and prevent elapsed-time mutation during pending task handoff. The experimental
nativeactor-task probe failed before task initialization and supplies no accepted
behavior. It remains a documented research blocker, separate from passing tests.

Native task/world scheduling, general original-mission harvesting, complete
construction/upgrades/abilities, full scene ordering/effects and independent
gameplay/device/performance gates remain. Browser fixtures were removed; launcher
and existing save were retained. No native window or admission bypass was used.

## Latest Bounded Harvest And AI Reducer Round

**Phases 1-2 remain accepted; Phases 3-5 remain unaccepted.** The user request to
enable every phase in the actual original game is not fulfilled. See the
[independent round-8 decision](acceptance-round8-20260919.md) for the exact
distinction between configured fixture workflows and default mission admission.

Final gate: **1,534 passed, zero failed, four skipped**, clean typecheck/build.
Mirrored mode-0 bodies now use the live native mask adapter with original
framebuffer/crop goldens. Mode-1 shadows, effects and global ordering remain
fallback, not accepted native composition.

Bounded player harvesting is wired through view/session/simulation: visible
VENT command, native movement in eight straight directions over 1-3 cells,
deployment, income, Stop/retraction and renewed movement with the same identity.
Checkpoints preserve each phase. A real embedded source-separated fixture earned
352 from zero, spent 350 and produced one troop, then restored and moved again.
No funds grant supplied that purchase. It remains an explicitly configured,
seeded actor fixture; the unchanged original loader does not attach nativeHarvest.

Review fixes isolate staged combat intents, reset successful native command modes,
and cache only cursor previews. A browser-discovered shorter-direction FIN issue
is fixed using the native looping-bank reset. Both races and slot orders pass
per-update animation tests. The full UI fixture was removed after desktop/mobile
checks, leaving the existing save and launcher unchanged.

Native-backed TypeScript AI now includes calculated initialization, assignment,
observation, a bounded group-1 call, and all 18 ordered demand predicates/actions
with typed production intents. The full scheduler/group/production-receipt cycle
is still incomplete; mode 3 remains rejected. Native golden computations are not
an automatically enabled original-mission policy.

Remaining acceptance work includes actual original-mission active AI, shared
task/occupancy/RNG ownership, general harvesting and harvester combat, complete
construction/upgrades/abilities, full scene effects, later campaign progression,
ordinary-input/native-reference/device checks and sustained rendering/memory gates.
Passing more helper trajectories did not waive any of those requirements.

## Latest Native-World And Mask Round

**Phases 1-2 accepted within their existing scope; Phases 3-5 still partial.**
Final gate: **966 passed, zero failed, four skipped**, typecheck/build clean.
The [round-7 addendum](acceptance-round7-20260919.md) supersedes historical
initialization blockers and pending counts in earlier reports.

Native source initialization now includes city construction, team dependencies,
relations, transport, PTH and explicit policy seed setters. The oracle executes
14,160/1,200 original world updates and natural target scheduling; seven ALIEN
orders are consumed in a subsequent world. HUMAN's target produces no actor
consumption. This is reference evidence, not an implemented TS active policy.

Native normal-body terrain masking is live under source slot/generation,
zero-height and integer-position constraints. The real embedded fixture recorded
one clip/37 spans and nonblank indexed terrain; unsupported modes/interpolation
and global ordering remain diagnostic fallback. No complete native visual parity
is claimed.

Neutral resource sources, canonical clocks/credits, eruption audio, checkpoint
options, external actor ownership and atomic session bind/release are integrated.
Native idle/wait and one bounded harvester corridor match original per-visit
traces, including Stop and non-centered arrival. General routes and complete
public-command handoff remain missing; Harvest is not falsely enabled. Ten pure
TRO/palette passes versus four full source-loader passes are now distinguished.

The actual TS AI, general resource/task/RNG ownership, complete production and
abilities, projectile/cadence/destruction fidelity, scene effects and independent
gameplay/device/performance gates still prevent all-phase acceptance. All browser
fixtures were disposed and the shared tab left at the launcher.

## Latest Ordered-Feedback And Playthrough Round

**Phases 1-2 remain accepted; Phases 3-5 remain partial, not accepted.** The
[independent round-6 review](acceptance-round6-20260919.md) distinguishes newly
closed subgates from those still missing. Final gate: **866 passed, zero failed,
four skipped**, clean typechecking and successful production build. A final CSS
build retry resolved a transient generated-output ENOTEMPTY error.

- Complete unchanged HUMAN01 and ALIEN01 each reached victory and defeat through
   legal public selection/movement/attack commands. No injected units, damage,
   funds, enemy orders or visibility overrides were used. Each outcome replayed
   exactly. Win ticks: Human 4769, Alien 7241; loss ticks: Human 2481, Alien 2465.
   This is [command-API evidence](source-playthrough-20260919.md), not ordinary
   browser input, graphics/audio acceptance or original-engine parity.
- Ordered trigger execution now observes staged world effects inside one block
   and across subsequent conditions. Late failure rolls back world/runtime/lives
   and production effects. Adapter commands are detached; sparse receipts reject;
   accepted receipts are snapshotted before callback reuse can mutate them.
- Raw source SCN bytes are hash-checked in the loader. Base troop production
   options, visits, purchases, panel and exact native creation bindings are live.
   Production-event checkpoint provenance is validated and both race fixtures
   restore after completion. Original first missions have no owned factories;
   synthetic funded browser tests are not counted as native mission economy.
- Objectives now cancels captured gestures and rechecks input blocking at pointer
   release. Rendering uses one shared snapshot per frame instead of copying the
   full simulation per child. The embedded modal regression and mobile production
   layout pass, with explicit synthetic input/fixture scope.
- Native AI probes execute original scans to 14096/1136 and all four groups.
   Alien records seven actor task consumptions at a frozen activation clock;
   Human empty packets remain insufficient. Relation-object initialization at
   game+0x471a0/+0x471a4 blocks complete alliance/visibility/combat history.
   Admission remains false. Scene clipping/order and queue provenance have native
   framebuffer tests, but complete caller admission/masks/effects remain unwired.
- Current reproducibility: **3,630 files / 482,845,897 bytes**, exact match;
   all 108 exported raw SCNs roundtrip with unchanged parsed fields. Only scenario
   envelopes changed, not index identities or other asset families.

All-phase acceptance remains blocked by full native AI/world history, general
mobile/resource task transfer and live income, complete production/construction/
upgrades, shared Inspire task/RNG ownership, projectile/cadence/destruction
fidelity, live scene effects, later-campaign workflows and independent device/
render-performance/reference testing. None was converted to a no-op to obtain
a green gate. Browser probes were removed; launcher and existing save remain.

## Latest Save And Session Round

**Phases 1-2 remain accepted within their existing source boundary; Phases 3-5
remain unaccepted.** This addendum supersedes older statements below about
missing checkpoint APIs, replay-only saves, or entirely absent production/resource
session owners. It does not promote opt-in fixtures into completed campaigns.

Final gate: **782 passed, zero failed, four skips**, plus clean typechecking and
production build. Native hit/static/Inspire reference files were supplied. The
shared embedded browser verified actual IndexedDB Save -> Exit -> Continue,
tick/unit/route identity, saved group recall and desktop/mobile-width layout.

- [Direct checkpoints](campaign-session-checkpoint.md) preserve simulation,
   native session/transport/resource/production state and view identity. New saves
   do not replay elapsed ticks; legacy replay saves migrate on next write.
   Save size in 20,000-idle-tick fixtures stays essentially constant. The
   [save UI](mission-save-ui.md) uses one atomic 32-MiB-limited IndexedDB slot.
- [Bounded diagnostic retention](session-retention.md) keeps 4,096 session frames
   by default. A 10,000-frame source run retained approximately 569 KB of journal
   JSON; its checkpoint grew four bytes since frame 1,000. Semantic creation/
   death/receipt/message history remains data-dependent, not hard-capped, so an
   indefinite combat-memory plateau is not claimed.
- [Resource session integration](resource-session-handoff.md) now drives the
   native-verified deployment, settlement, cancellation and depletion/removal
   path using explicit source animation, clock, AI scaling and credit-gate inputs.
   It preserves transactional economy and restores in-flight state. General
   mobile task ownership and arbitrary combat interruption remain unsupported.
- [Production sessions](campaign-production-session.md) support actual source
   TRSC/GRAY producer FIN clocks, credit reservation/accounting, occupied-exit
   waiting, population refunds and atomic exact-exit allocation. The reserved
   `1022` exit bug was reproduced and fixed: production no longer spawns beside
   its reserved tile. Native startup [source options](source-production-options.md)
   cover initial restrictions, queues, census and cap without inventing funds.
   The opening player teams own no factories; neither mission was modified to
   provide one. Other producers, construction/upgrades, destruction and live
   campaign HUD/source scheduler integration are not accepted.
- [AI consumer research](ai-policy-runtime-20260919.md) now covers additional
   native group callbacks and source-initialization orders, but not complete
   activation-time trigger/task history. Mode 3 still fails explicitly; no
   unused selector field or generic aggression policy was substituted.
- [Performance evidence](battle-performance-20260919.md) exercises active
   128/256/512-unit synthetic battles for 120 simulated seconds and bounded source
   moving sessions. CPU percentiles passed the declared harness thresholds, but
   strict worst-case updates had failures. No GPU FPS, sustained source battle,
   WebKit/iOS, ordinary-input mission completion or original-game visual parity
   is implied.

The remaining gates require native global scheduling/AI integration, complete
production/extraction workflows, Inspire host state, projectile/cadence and
construction-death fidelity, rendering effects and independent end-to-end/device
evidence. These are recorded blockers, not completed work hidden behind labels.

## Latest Multiagent Decision

**Phases 1 and 2 accepted; Phases 3-5 remain unaccepted.** This section supersedes
older snapshots below. Phase 2 is accepted only within the pinned shipped-source
scope, with explicit malformed-source and dependency exclusions justified in the
independent [Phase 2 decision](phase2-acceptance-20260919.md). No native gameplay
or rendering requirement has been waived to obtain that decision.

Latest full gate: **655 passed, zero failed, three explicit skips**, plus clean
TypeScript and production build. The run supplies the fresh hit, static-occupancy
and Inspire native reports; a separate [reference run](native-reference-round4.md)
passes 80 tests with zero skips. The full-gate skips are two optional external
Chromium checks and a separately conditional fire-cycle trace comparison.

Current published corpus: **3,630 files / 482,395,625 bytes**, exactly reproduced;
1,438 JSONs and 2,737 index/source checks per corpus match. The active indexed
generation is `b86dde044988163b2b255f584b124e7cc74af59564c312b6faaefccacf68647c`.
This includes source MBULLET, four verified terrain banks/108 scenario palette
metadata, four CD MP3 alternatives and 259 short-effect PCM references.

Live integrations added in this round:

- Source class/armor damage for both opening rosters, initial scenario weapon and
   armor levels, original type-faction phase modifier evaluated per hit, upgraded
   weapon audio IDs, and beacon defense updates. Alien01 team-0 Grays start at
   levels 1/1. Native shot/flight/cadence fidelity remains distinct.
- Noncolony ground occupancy from proved native definition fields, separate from
   air/auxiliary/neutral occupancy. Browser overlap-safe cleanup is not equated to
   native overwrite/first-match removal.
- Dispatch-time `setarray`, zero omitted lives, transactional `newrate`, persistent
   full-width resource scales, special type-40 state, RENAT nonentity placement
   allocation and native five-pair City parsing. AI modes, resource extraction/
   depletion, production and RENAT spawning are still explicit unsupported paths.
- All four palette banks are loaded with verified manifest/payload checks.
   Ten mission data sets pass preflight, not ten playable missions.
- WebAudio codec fallback uses bounded exported PCM alternatives, preserving the
   source sample payload. No requests access raw source paths at runtime.

Embedded browser evidence: all **8,192 RGBA tile pixels** matched native palette/
remap expectations across four banks and two phase endpoints; textures were
released. Upgraded Alien01's live hit matched the expected phase-adjusted damage.
All **259 PCM effects decoded**, versus zero Opus effects in this embedded build.
Selection, acknowledgment and weapon cues played with non-silent analyzer output,
one WAV retry each, cached replay without fetch, and clean stop/disposal. This is
API-driven browser QA, not physical-speaker, ordinary-input, WebKit or campaign
completion certification. Browser probes were removed and launcher restored.

Inspire's native lifecycle is now proved and its opt-in simulation adapter passes
goldens, but the mission lacks complete native task-order/shared-RNG/occupancy
hooks. The star remains disabled rather than wiring no-op hooks. Native AI mode 3
has bounded callback evidence but no full policy implementation. Construction's
legal lethal-departure cleanup still requires a valid original-game reference;
normal/timed-nonlethal passes do not close it. The original-game Wine window was
not launched because explicit approval to leave embedded-only testing was not
available. Full save/restore, later-mission workflows, native effects/shadows,
device/reference tests and sustained battle performance remain unmet gates.

## Final Orchestrator Addendum

This addendum supersedes the earlier snapshot below where explicitly noted.
The final integrated `npm run check` passed **416 tests, zero failed, two
optional browser tests skipped**, plus typecheck and Vite production build.
Editor diagnostics are clean. This does not accept phases 2-5.

- Current reproduction is **3,267 published files / 469,116,548 bytes**, after
   adding four deterministic MP3 CD outputs from original PCM and updating two
   media indexes. The prior clean full reproduction was extended with an isolated
   CD-only export; 2,266 indexed checks per corpus pass. See the updated
   [asset reproduction evidence](asset-reproduction-20260919.md).
- [Runtime dependency evidence](asset-runtime-dependencies-20260919.md) excludes
   the eleven unsupported FINs, two invalid FINs and twenty missing-sprite
   diagnostics from the audited shipped manifest closure. It does not decode
   those files or prove the short multiplayer RMP unnecessary. Phase 2 remains open.
- [Native CD calls](native-cd-audio-20260919.md) establish mission 02-05 ordering
   and strict greater-than-5000-ms stopped polling. This is now wired through one
   streaming media element; the earlier no-live-CD statement below is superseded.
   Real embedded playback rejected valid Opus, then succeeded through MP3 fallback.
   Actual media end events (accelerated by seeking) advanced 02 -> 03 -> 04 -> 05;
   mute/unmute preserved the stream and mission exit stopped/cleared it. No
   physical-speaker, gapless, iOS/WebKit or ordinary-gesture acceptance is claimed.
- [Construction lifecycle](construction-lifecycle-20260919.md) now executes real
   source FINs and uninterrupted normal main/auxiliary paths. Its 24 passing
   checks do not resolve legal destruction/cancellation cleanup. Two death
   components remain blocked, six negative controls are retained, and the probe
   deliberately exits 1. It is not part of the passing application suite.
- Team-aware live targeting, shared world/radar fog, static radar markers and
   bounded next/retry integration remain verified. Human02 is explicitly rejected;
   unsupported actions are never filtered out. The shared embedded tab was returned
   to the mission launcher after removing probes.

**All-phase acceptance is still blocked**, primarily by production/upgrades and
resource host state, dynamic combat/ability lifecycle integration, later mission
actions, remaining native rendering behavior and end-to-end reference/device/
performance evidence. Neither the application tests nor research components
substitute for those gates.

## Decision

**Phase 1 remains accepted. Phases 2-5 remain partial, not accepted**, exactly
as in the current [architecture phase table](../ARCHITECTURE.md#phase-gates).
This preserves established acceptance; it neither downgrades ingestion nor
renames incomplete gates as accepted. Supported asset reproduction passes its
own bounded gate. Live opening-mission integration is substantially beyond the
historical missing-transports/statics/colony/AI assessment, but is not a complete
native game or an ordinary-input campaign completion certificate.

This report owns only this new document. It independently inspects current
source call sites and reconciles the ten requested evidence documents. It runs
no agents, browser, native probes, asset regeneration, or test suite, and changes
no shared source. The orchestrator's full `npm run check` is in progress at this
handoff; **its final result and count belong to the orchestrator and are not
asserted here**.

## Evidence Boundaries

| Evidence | What it establishes | What it does not establish |
| --- | --- | --- |
| Independently inspected current source below | Live call paths, supplied inputs, helper-only boundaries and explicit rejection | Successful execution, native parity or ordinary-input completion |
| [Clean asset reproduction](asset-reproduction-20260919.md) | All 3,263 published files / 454,474,203 bytes reproduced exactly; all 1,331 JSON structured hashes match; no published additions, omissions or changes | Support for rejected formats, correctness of unknown semantics, or runtime fidelity |
| Native research reports | Executed original instruction slices with stated fixtures, intercepted calls and stopping boundaries | Complete original-game boot, uninterrupted lifecycle or browser integration |
| Focused helper/integration tests reported in the documents | Their specified contracts and controlled branches | A user winning a mission through ordinary controls |
| Latest orchestrator browser API/pixel results, supplied with this task | Unknown fog black, explored terrain retained, Next Mission reaches explicit unsupported HUMAN02 error, diagnostic reachable at 390x844 | Independently rerun checks by this author, ordinary-input victory, mobile-device/WebKit coverage or playable HUMAN02 |

Reproduction is against the supplied on-disk corpus, not an established Git
commit. The reproduction report records no Git metadata and pins its own
source/code snapshots. Its exact file count is asset evidence, not a test count
and not a claim that later concurrent runtime edits were part of that snapshot.
Reported native case counts are likewise not substituted for the pending full
gate. No ordinary-input win is claimed.

## Phase Gates

| Phase | Retained status | Current evidence and exact boundary |
| --- | --- | --- |
| 1: Ingestion | Accepted | Existing MDF/MDS geometry, source hashes, 5,151-file extraction and deterministic manifest evidence retained. Clean reproduction independently strengthens the supported pipeline. Repeat provenance checks if source changes. |
| 2: Assets | Partial, not accepted | 284 SPRs, 164 standard FINs, four BTS banks, 108 map bundles and standard media reproduce. MAP/BTS, SCN, DESERT initialization and indexed extraction are verified within documented scope. Eleven legacy FINs, two invalid FINs, the short multiplayer RMP, unresolved bindings and general static semantics remain. |
| 3: Simulation | Partial, not accepted | Opening-mission controller, transport delivery/pickup, native identities, colony projection/footprints, trips, deaths, outcomes and source day/night are live. Source team/alliance wiring is now live too. Native production/economy/upgrades, impact/cadence integration, wider AI and general static cleanup/footprints remain incomplete. |
| 4: Presentation | Partial, not accepted | Indexed terrain, FIN composition/team colors, responses, radar/groups/routes, results and progression controls are live. World/radar share retained exploration and radar includes source static targets. Native effects/shadows, global child sorting, construction/action HUD, Inspire lifecycle and native ambient/CD scheduling remain open. |
| 5: Testing | Partial, not accepted | Controlled mission outcome fixtures and reported browser API/pixel checks are useful evidence. Ordinary-input wins/failures, playable progression through missions 02-15, WebKit/device checks, original-reference comparisons and battle-scale FPS/memory remain unaccepted. Await the orchestrator's final full gate separately. |

The architecture's older statement that Inspire effect/cooldown is unverified
needs the narrower reading established by [the fresh Inspire research](inspire-runtime.md):
core component semantics are now evidenced, but the native lifecycle and live
integration are not. Likewise, [the newer combat caller audit](combat-callers-20260919.md)
supersedes the unresolved caller-factor/flag statement in
[the earlier combat helper document](native-combat-fidelity.md) only for its
explicit bounded subset. Neither correction accepts the containing phase.

## Independently Inspected Source

These are current file/symbol checks performed for this report, not copied
claims of runtime absence from a historical audit.

| File and symbol | Verified current behavior |
| --- | --- |
| [src/mission-view.ts](../src/mission-view.ts), `MissionView` constructor and `#registerEntity` | Supplies `teamAlliances` from SCN `allies`; supplies `team: entity.team` on mobile and static registration. Uses `createCampaignSession`, `transportHostState` and `projectLegacyColony`; colony static targets receive projected footprints and source HP. Source team race determines faction. |
| [src/mission-view.ts](../src/mission-view.ts), `#stepSession` / `#applySessionFrame` | Bridges reservations, positions and authoritative combat deaths to the session; applies create/noncombat removal requests; maintains slot/generation bindings, carriers, messages, waypoints and delayed outcomes. Transports and statics are not absent. |
| [src/mission-view.ts](../src/mission-view.ts), `#recordExploration`, `visibility`, `explored` | Retains visible cells in a mission-owned mask; calls exploration recording in the simulation-advance path. `explored` returns a copy. Visibility currently uses owned living mobile units and source sight ranges; this is not proof of native allied sight sharing or observation geometry. |
| [src/main.ts](../src/main.ts), `updateMissionRadar`, `startCampaign`, result-action handler | Passes the mission's visible/explored masks and `snapshotRadarEntities` into radar. Loads the requested mission number, checks initialization diagnostics before activation, and exposes the explicit result action. Progression is live UI wiring, not just a helper. |
| [src/ui/radar.ts](../src/ui/radar.ts), `snapshotRadarEntities`, `radarMarkers`, `drawRadar` | Includes simulation static targets as building markers. Dead targets are excluded; nonowned targets require current visibility. Unknown terrain is painted opaque black with outward-rounded cells; explored unseen terrain is dimmed before marker/view drawing. Retained terrain does not imply retained enemy markers. |
| [src/game-data.ts](../src/game-data.ts), `campaignMissionStem`, `campaignResultAction`, `campaignPreflight` | Enforces faction/01..15 bounds; ready success offers next, other ready results retry, pending results offer neither, success at 15 does not wrap. Audits every block's world actions and enforces DESERT-only verified palette support. `CampaignMissionData` has no production catalog; the team interface has no dependency-restriction field. |
| [src/engine/legacy-balance.ts](../src/engine/legacy-balance.ts), `unitOptionsFromLegacy`; [src/engine/simulation.ts](../src/engine/simulation.ts), `#advanceAttack` | Source damage/defense requires the optional combat profile and both registered sides. MissionView calls the adapter without that profile and registers statics without source defense, so live mission hits take flat weapon damage. Cooldown still uses `rateOfFire` as browser ticks. A source-backed arithmetic helper exists, but missions do not opt in. |
| [src/engine/legacy-production.ts](../src/engine/legacy-production.ts), `createLegacyProductionCatalog`, `checkLegacyProductionRequirements` | Catalog preserves costs/dependencies and explicitly returns `buildTimeTicks: null`. Requirement checking is a pure catalog/credits/set calculation, not native busy/latch/FIFO eligibility. Source search finds no live catalog call. MissionView initializes generic resources under a faction key, not an authoritative per-team production account. |
| [src/engine/legacy-inspire.ts](../src/engine/legacy-inspire.ts), `scanLegacyInspireEffect`; [src/audio/soundtrack.ts](../src/audio/soundtrack.ts), `MissionSoundtrack` | Source-reference inspection finds definitions but no live application call/instantiation. The documented Inspire helper and explicit-playlist scheduler must not be counted as enabled mission features. |

Alliances now influence simulation hostility, but ownership remains team 0;
the radar's owned/nonowned coloring is not a newly verified native diplomacy UI.
The existing deterministic guard/acquisition policy is live, as recorded in the
architecture. The remaining AI gate is wider native priorities/script behavior,
not a claim that the browser has no AI at all.

## Prioritized Remaining Blockers

Priority here orders the next integration work, not permission to waive the
independent asset or verification gates.

1. **Native production/economy transaction.**
   [Production research](production-runtime-audit.md) proves that building
   completion is animation/phase driven, not a guessed countdown. Integrate
   per-team credits and cost accumulator, UI reservations, four typed FIFOs,
   reserved colony identities, HP/level/busy, construction latch, restrictions
   and upgrade state through one authoritative owner. Real FIN timing,
   auxiliary phases, exit reservation/spawn, blocked exits, population limits,
   destruction/refunds and harvesting/deposit accounting remain unverified or
   unintegrated. Add the actual DEPEND action/status UI only against that
   contract. The current parser is already corrected: the audit verified all
   80 dependencies and 106 health values; old parser-corruption claims are not
   current blockers. Initial colony projection is already live, not missing.

2. **Mission combat and Inspire lifecycle.**
   The newer caller probe establishes uninspired factor 256 and a source-type
   faction/native-phase penalty at impact for specified TRSC/GRAY direct hits.
   For base weapon IDs 1/15, verified neutral-level damage is 25/18 against
   class-0 TRSC/GRAY and 4/3 against listed class-9 buildings in favorable/adverse
   phases. These are resolved-hit arithmetic fixtures, not complete shots.
   Supply real MBULLET/class/armor data, prove initial scenario levels, and
   evaluate the phase predicate at impact; a fixed registration flag cannot
   span a phase flip. Scenario loading can override the fresh zero levels.
   Cadence, flight/hit/splash selection, dynamic upgrades, source-slot lifetime
   and other callers remain separate. Inspire additionally needs uninterrupted
   animation/task continuation, interruption/recast behavior, caster death/type
   change/slot reuse, and expiry ordering across the resetting native clock.
   Its visit budget is not a distinct-unit count and its payload 50 is not a
   proved cooldown. Do not enable it as an immediate guessed buff.

3. **Real mission02 host state and remaining actions.**
   [Campaign progression](campaign-progression.md) reports preflight admission
   of only HUMAN01/ALIEN01, 2 of 30 numbered missions, not 30 playable missions.
   HUMAN02 rejects `newrate` in TRO 8/16, `ai` in 17 and `setarray` in 18.
   ALIEN02 rejects missing explicit lives in TRO 8, `ai` in 0, `newrate` in 10
   and `setarray` in 11. [The newrate audit](mission02-action-audit.md) establishes
   its byte operands, first active type-40 selection, synchronous rate overwrite
   and activation sound, but not initialization/consumption of that rate or
   full-width `s(1,0)`. Special SCN rows use fourth-column values 22/25, which
   cannot be treated as ordinary team IDs. Preserve those source records and
   prove their native fields; never invent scale 256, add lives, drop dormant
   actions or accept missing targets as no-ops. Later campaigns also require
   additional action/condition forms and JUNGLE/ATLANTIS/HTRAIN initialization.
   The explicit unsupported-next-mission screen is correct failure handling,
   not campaign advancement acceptance. Save/unlock persistence is absent.

4. **Legacy assets and native presentation.**
   [Legacy variants](phase2-legacy-variants.md) still leave exactly eleven tag
   -3 lighting FINs unsupported and ANIM.FIN/BUILDING.FIN invalid. Twenty
   `missing-fin-sprite` diagnostics remain. All 20 standard RMP tables are
   supported, but the 67,584-byte multiplayer RMP is not; the indexed export
   publishes five applicable standard root triples, not all 20 source tables.
   Neither retagging FIN nor padding/repeating RMP bytes is evidence. Find the
   actual legacy producer/consumer, prove layout and runtime addressing, and
   retain explicit diagnostics meanwhile. General non-colony footprints/static
   semantics, cleanup fidelity, native shadow/palette effects and cross-entity
   FIN child sorting remain open despite working indexed terrain and colonies.

5. **Native audio policy and end-to-end verification.**
   [Audio scheduling](audio-scheduling.md) implements an explicit playlist
   mechanism only. Disc track identity proves neither mission assignment nor
   start/order/repeat/transition policy; no automatic mission soundtrack is
   wired. Long decoded tracks exceed the default 32 MiB budget, and fake-audio
   tests do not certify real decoding/audibility/device behavior. Obtain native
   call-site evidence and choose bounded memory/streaming behavior before
   wiring a native default. Complete ordinary-input opening-mission wins and
   failures, real progression, WebKit/iOS audio/lifecycle checks, native visual
   references and sustained battle FPS/memory tests. Embedded API/pixel passes
   and synchronous render-work timings do not replace those gates.

## Next Discriminating Native Check

**Recommended first: finish one real building's construction lifecycle before
implementing a campaign production timer.** Extend the existing
[production probe](../tools/research/production-audit-20260919.py) under the
same executable hash gate. Resolve a real constructible type's FIN profile and
execute its auxiliary type-92/93 transitions alongside the main building using
the native update dispatcher, through final team-latch release. Remove the
relevant auxiliary-dispatch/continuation interceptions for this sequence.

The falsifiable hypothesis is that source frame timing plus auxiliary lifecycle
drives the distinct slot-busy and team-latch transitions; elapsed synthetic
ticks or nonzero HP alone cannot certify readiness. Record source profile,
frame/delay, main and auxiliary phase, animation state, slot busy, team latch,
HP and update count at every transition. Exercise the initialization counter
3/4 boundary, normal completion and interruption. Fail on any unexplained
transition or missing completion; compare with the existing synthetic
`sum(delays)+1` fixture only within its stated initialization assumptions.
If real dispatch does not match that model, revise the owner contract before
adding UI/countdown behavior. Then probe blocked troop exits, population and
active deletion before accepting a production workflow.

For the mission02 branch, the next distinct probe is the native SCN type-40
loader into its rate consumer: establish fourth/fifth-column meanings,
initialized `s(1,0)`, rate units and later credit writes using the actual mission
rows. Passing `newrate`'s already verified action slice again would not close
that gap or justify admitting either whole mission.

## Handoff

Retain Phase 1 acceptance and the exact supported-corpus reproduction pass.
Retain explicit unsupported-mission/asset diagnostics. Credit the live
team/alliance, fog/radar/static and progression integrations without claiming
native parity or ordinary-input victory. The orchestrator must append its own
final full-check result and any additional browser evidence in its handoff;
this report deliberately supplies no invented final gate count.