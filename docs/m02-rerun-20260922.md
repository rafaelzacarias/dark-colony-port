# Bounded Adapted M02 Rerun

Historical runs below predate the latest performance changes. See the
[independent outcome rerun](m02-endjudge-20260922.md) for the subsequent actual
M02 results and the first newly observed runtime blocker.

Scope: QA strategy, harness, tests and documentation only. No engine/runtime,
UI, native gates, source objectives, assets, agents, browser or full-suite changes.
All runs use the actual original loader with `browser-adapted`, source production,
public player commands and finite-source economy. No health/credit/fog writes,
enemy orders, teleports or native input injection.

## Literal Source Goals

The parsed original scripts, not mission prose or assumed building counts, own
the results. HUMAN02 trigger 6 requires `(s(2,3)>26)`: 27 team-2 victim losses.
ALIEN02 trigger 4 requires `(s(1,0,86)>5)`: six team-1 communication-site losses.
Neither M02 script contains trip blocks. HUMAN's loss is all five player building
slots empty or one player type-86 loss. ALIEN's loss is all five player building
slots empty. Commander death alone triggers recovery in both scripts, not LOSS.
Eight focused tests authenticate these conditions against raw TRO/SCN files.
HUMAN has ten initial team-2 placements and exactly one team-2 reinforcement:
ten type-8 plus seven type-10 actors at `c>880`. Those 17 are needed to reach 27;
the current ordinary mission cannot satisfy that census with the initial ten.

## Verified QA Infrastructure

- Deadlines: at most 50000 ticks and 900 seconds per simulation attempt; default
  30000 ticks. Every 1000 ticks emits progress and elapsed wall time. The launcher
  has a hard timeout. No unbounded 300000-tick continuation.
- Actual source image initialization with NullCanvas search or sampled render
  dispatch. Sampled mode also renders observed combat, delivery and carrier events.
  This verifies PNG headers/assets and render dispatch, not raster pixels or GPU.
- Public-command journal replay, view/planner checkpoint persistence and resume.
  Checkpoints are QA files under `/tmp`, never user saves. Restore validates the
  runtime's own checkpoint; the harness does not modify it.
- Separate delivered/published/spent/credit audit. Pending delivery is explicit.
- Adaptive paid infantry goal (up to 40), source-aware objective coordinates,
  source static obstacle footprints, commander protection and producer-exit guard.

Short proof: `/tmp/dc-m02-qa-checkpoint-17901123.log` validates ALIEN checkpoint
round-trip at tick 80. `/tmp/dc-m02-qa-resume-17901126.log` resumes it to 100.
`/tmp/dc-m02-qa-replay-17901126.log` replays its public command with render every
tick. These are UNKNOWN mission outcomes, not wins.

The 600-tick HUMAN public-command replay is
`/tmp/dc-m02-human-win-wnJR1u/journal.jsonl`:
80 shots, zero deaths, one paid 350-credit purchase, eight command events,
506 delivered/published income, 156 credits. It matches the original trace with
sampled real-asset render dispatch for combat/resources/carriers. No WIN claimed.
ALIEN's fresh 600-tick smoke delivered 575, published 550, spent 350, retained
200 credits and had 25 pending. Its journal is
`/tmp/dc-m02-alien-win-JaLua8/journal.jsonl`.

## Runs And Limits

An inherited 45-minute process was identified by exact PIDs and stopped before
replacement. Its latest report was around tick 6500, without a completed result:
`/tmp/dc-m02-existing-compact-r1.json`.

The first bounded ALIEN run ended at tick 3346 with the 900-second QA deadline,
137 shots, three deaths, zero income and no outcome:
`/tmp/dc-m02-alien-win-VdVK5z/journal.jsonl`. Its loaded MissionView hash was
`3480179ffe6a4b21dc304fe435204f6d00ce993934c857275fc5f3362f1b23a5`.
The later fresh smoke loaded a different runtime revision and restored mining;
this stale attempt is not evidence about current harvesting. Source mission
hashes remained identical. Concurrent external runtime edits were not reverted.

The initial HUMAN commander-only run reached tick 4075, 659 shots, eight deaths,
zero income/spending and no ready outcome, then failed its 900-second deadline:
`/tmp/dc-m02-human-loss-sDaERG/journal.jsonl`.

Fresh 1000-tick controls with checkpoint round-trip both returned UNKNOWN and
correctly failed required LOSS assertions: HUMAN had 340 shots, three deaths,
four command events and zero income/spending; ALIEN had zero accepted movement
commands because the target was an occupied source site. Journals:
`/tmp/dc-m02-human-loss-SUYwVN/journal.jsonl` and
`/tmp/dc-m02-alien-loss-oEtcu8/journal.jsonl`. The latter exposed a QA routing
defect, subsequently changed to reachable cells near the source target. No source
loss condition or expected result was weakened.

## Runtime Handoff

The previous tick-711/tick-2002 casualty overwrite is not reproduced in the
bounded post-fix traces: casualty checks continue past those points. This is not
yet proof of a completed casualty-based victory.

NullCanvas does not remove the dominant increasing frame cost. HUMAN's later
win run took 80s at tick 1000, 261s at 2000, 522s at 3000 and 856s at 4000.
The read-only local path is [MissionView staging](../src/mission-view.ts#L489),
which restores a simulation checkpoint and forks the session each adapted tick,
and [CampaignSession fork](../src/engine/campaign-session.ts#L1446), which clones
retained journal entries. This is a concrete profiling lead, not measured
exclusive attribution. Reaching the natural HUMAN activation at 14096 is not
established within the public-view 900-second bound. No QA bypass, shortened
source clock, removed history or runtime performance edit was applied.

## Completed Checks

The isolated original HUMAN activation test passes freshly at tick 14096,
including unchanged source, selector 4-to-3, reinforcement, exact JSON restore
and eight continuation ticks. Runtime: 455.2 seconds. This is the session-level
test, not a public-view combat victory:
`/tmp/dc-m02-human-activation-17901134.log`.

The corrected ALIEN commander route accepted 14 public movements through tick
400, then resumed its actual checkpoint. The bounded continuation reached tick
1465: 203 shots, one death, 30 total commands, no income or purchases, no outcome.
Its `0,0,73` casualty counter is 1 and recovery array `0,2,0` is 134. It stopped
at the requested shorter 180-second simulation deadline, classified FAIL, not
LOSS. Journal: `/tmp/dc-m02-alien-loss-aGsD3t/journal.jsonl`. Its render samples
include 203 combat and 18 carrier frames. This directly demonstrates why the
commander-only control is not a ready-loss proof for the unchanged ALIEN02 TRO.

The HUMAN 600-tick replay sampled 80 combat, 23 delivery and 16 carrier frames.
Eight focused unit tests and final strict/noUnused QA compilation pass:
`/tmp/dc-m02-final-source-unit-r13.log` and
`/tmp/dc-m02-final-owned-types-17901137.log`.

## Final Victory Attempts

HUMAN: FAIL at tick 4110 on `QA wall-clock deadline exceeded (900000ms)`.
707 shots, 18 deaths (nine player infantry, six goal-team soldiers, two team-3
soldiers and one team-1 site), 49 command events, 12 purchases. Objective 6/27;
income delivered/published 4356, spending 4200, credits 156. Currency balances,
original mission is unchanged, commander remains alive, outcome is null.
Journal and checkpoint: `/tmp/dc-m02-human-win-DoSizD/`.

This long run loaded the strategy before the later footprint-index conversion
fix. It is a valid legal-command/economy/death/deadline trace, not validation of
the final full-footprint routing policy. Short final-policy probes, typechecks
and replay checks are separate. No corrected-policy completed victory is claimed.

ALIEN: FAIL at tick 3241 on `QA wall-clock deadline exceeded (900000ms)`.
162 shots, two team-1 infantry deaths, 86 command events, seven purchases.
Objective 0/6; delivered/published income 3875, spending 2450, credits 1425.
Currency balances, original mission is unchanged, outcome is null. The two
original type-41 turrets retain source damage 100, range 6, cooldown 15 and
their damage coefficients; 19 actual turret shots were observed. Original team-1
activation occurs and is journaled at the next observation, tick 1140.
Journal and checkpoint: `/tmp/dc-m02-alien-win-Q6rOtN/`.

Both completed attempts use the original `browser-adapted` source production,
economy and AI configuration, not the native combat owner. Their initial/final
mission hashes match. Full provenance, nonzero casualty counters and currency
audits are in [the result artifact](m02-rerun-results-20260922.json).
The combined runner exits 1, as required. No playthrough process remains active.

Final acceptance: **FAIL / incomplete**, not a victory pass. Neither ready WIN
nor ready LOSS was obtained. The fixed casualty-publication path survived the
old failure points; legal delivery and paid FIN production worked, and real
turret fire was present. The public-view deadline prevents a complete strategy
search or victory replay in this environment. The exact main-owner handoff is
the growing per-frame staging/fork/history cost above, with the completed
timestamps and counts provided; there is no new source-runtime diagnostic in
these final two runs. Army tactics against all sites remain unproven.

No winning actual-asset replay, raster validation, final result UI or campaign
progression is claimed. Do not change the objective assertions or label the
commander recovery as LOSS to turn this result green. The next legal run needs
the unchanged public runtime to reach the original source horizon within its
budget; QA must not bypass the owning transaction or mutate retained history.